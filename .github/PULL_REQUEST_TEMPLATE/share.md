<!-- Auto-filled by `sailor share`. Reviewers: confirm no secrets, valid mandate, sane strategy. -->

## What is this?

<!-- summary + description from .sail/share.json -->

## Review checklist

- [ ] `projects/<slug>/.sail/share.json` present and complete (strategy + mandate described)
- [ ] No secrets: no `.sail/keys/`, `account.json`, `.env.local`, `ci-keystore.json`, private keys, or RPC keys
- [ ] `src/agent.ts`, `src/mandate.ts`, and at least one `mandates/**/*.sol` present
- [ ] Strategy + mandate described match the code
- [ ] Slug is unique under `projects/`
